# 🧠 百科知识大闯关

网页版知识百科答题闯关游戏：内置 200 道小学 AI + 百科知识题，每关 5 道选择题，**全部答对才能进入下一关**，答错则本关重新挑战，边玩边学。

## 核心功能

- **闯关玩法**：每关 5 道选择题，全对过关；答错显示正确答案与解析并重来
- **内置题库**：40 关共 200 题（桌面文档《小学AI+百科知识题库》导入 + 补全），覆盖 AI 常识（提示词、AI 应用、隐私安全、AI 伦理）与百科知识（天文地理、生物科学、历史文化、生活常识），其中单选 116 道 + 判断 84 道（以「正确/错误」形式呈现）
- **学习模式**：浏览全部题目（按关卡切换），**答案默认隐藏**，点击「显示答案」即展开正确答案与解析，适合先复习再闯关
- **进度持久化**：关卡进度 / 已解锁关卡自动保存（SQLite），支持一键重置
- **答案解析**：每道题附带解析，答对答错都会展示，寓教于乐
- **即时反馈**：HTMX 无刷新交互，答完立刻出结果
- **音效反馈**：Web Audio 实时合成音效，答对 / 答错 / 过关 / 大满贯各有专属提示音，无需任何音频文件
- **炫酷科技感 UI**：深空星空背景、霓虹渐变标题、发光按钮与进度条，答对 / 答错 / 过关各有专属动效

## 技术栈

FastAPI + HTMX + Alpine.js + Tailwind CSS（FHAT），SQLite + SQLModel，零 Node 依赖。

## 运行方式

```bash
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

访问 `http://localhost:8000` 开始闯关。

> 提示：首次启动自动建表并写入内置题库。改 schema 后删除根目录 `app.db` 再启动即可重建。

## 纯静态版（`static-site/`，可直接发布到静态托管）

`static-site/` 是**完全纯静态、零后端、零依赖**的多文件版本，专为「比赛提交 / 外网发布」设计：

```
static-site/
├── index.html      # 首页：开始闯关 / 学习模式 / 关卡地图 / 进度统计 / 重置
├── game.html       # 闯关页：40 关 × 5 题，全对过关，答错本关重来
├── study.html      # 学习模式：浏览全部题目，可搜索 / 按关卡筛选，答案默认隐藏
├── css/style.css   # 科技感暗色主题
└── js/
    ├── data.js     # 200 题数据（由题库自动生成）
    ├── common.js   # 进度存储（localStorage）+ 音效（Web Audio）
    ├── game.js     # 闯关逻辑
    ├── index.js    # 首页逻辑
    └── study.js    # 学习模式逻辑
```

- **纯静态**：无 FastAPI / 无数据库 / 无构建步骤，任意静态服务器都能跑
- **双击即玩**：直接打开 `static-site/index.html` 即可
- **可发布**：可部署到 GitHub Pages / AtomGit Pages / Vercel / Netlify / Gitee Pages 等任意静态托管
- **进度本地保存**：闯关进度存浏览器 localStorage，换设备不共享（纯静态特性）

**重新生成题库数据**（`builtin_questions.py` 变更后）：

```bash
python gen_static_data.py
```

### 发布到 GitHub Pages

1. 把 `static-site/` 目录内容推到你的 GitHub 仓库（或单独建仓库）
2. 仓库 **Settings → Pages** → Source 选 `main` 分支 + 根目录（或 `static-site` 目录）→ Save
3. 稍等片刻即获得外网链接：`https://<你的用户名>.github.io/<仓库名>/`

### 发布到 AtomGit Pages

1. 推送 `static-site/` 到 AtomGit 仓库
2. 在仓库 **服务 / Pages** 里选择部署目录 `static-site/` 并启用
3. 获得 `https://<用户名>.atomgit.com/<仓库名>/` 外网链接

## 离线版（单文件，双击即玩）

根目录 `百科知识大闯关-离线版.html` 是**不依赖任何后端 / 网络 / 安装环境**的单文件版：

- 200 题全部内嵌（58 KB），双击文件即可在浏览器中游玩
- 完整保留闯关玩法（40 关 × 5 题、全对过关、答错重来）、学习模式、音效、科技感界面
- 进度自动保存在浏览器本地（localStorage），关掉再开不丢
- 适合拷给同学 / 老师 / U 盘带走，或本机离线游玩

重新生成方式（题库变更后）：

```bash
python tools_build_offline.py
```

## 部署到外网（Render，免费）

项目已内置 `render.yaml`（Blueprint 部署配置），把仓库推到 GitHub 后即可一键部署：

1. 将本仓库推送到 GitHub（`git remote add origin <你的仓库地址> && git push -u origin main`）
2. 打开 [Render 控制台](https://dashboard.render.com) 注册（支持 GitHub 账号直接登录）
3. **New +** → **Blueprint** → 选择你的 GitHub 仓库 → Render 会自动读取 `render.yaml`
4. 确认配置后点 **Apply**，等待 2~5 分钟构建完成
5. 完成后访问平台生成的公网地址（形如 `https://baike-quiz.onrender.com`），手机 / 电脑 / 任意设备都能打开

> 注意事项：
> - 免费实例休眠：Render 免费实例闲置 15 分钟会休眠，首次访问需等约 30 秒唤醒，属正常现象
> - 进度数据：免费实例文件系统是临时的，实例重建 / 休眠重启后闯关进度可能重置（代码与题库不受影响）。如需进度永久保存，可在 Render 控制台挂载持久磁盘或改用付费实例
> - 国内访问：免费节点可选 `singapore`（render.yaml 已配置），比美国节点更快

## 关卡结构

- 题库 200 题，每关 5 题 → 40 关，每关恰好 5 题
- 题目按文档顺序分关：AI 单选 → 百科单选 → AI 判断 → 百科判断

## 变更日志

- 2026-07-26：初版上线。闯关核心玩法、内置题库、进度持久化。
- 2026-07-26：界面升级为**深空霓虹科技感主题**。
- 2026-07-26：内置题库替换为桌面文档《小学AI+百科知识题库》全部题目并补全至 200 题，每关恢复 5 题共 40 关；移除「在线导入题库」模块；新增**答对 / 答错 / 过关音效**（Web Audio 合成）。
- 2026-07-26：新增**学习模式**（浏览全部题目、答案默认隐藏、点击显示）；首页「内置题库」卡片改为「学习模式」入口。
- 2026-07-26：新增**离线版单文件** `百科知识大闯关-离线版.html`（200 题内嵌、双击即玩、进度 localStorage 保存），并修复通关后按钮误显示「进入第 1 关」的问题（改为进入下一关）。
